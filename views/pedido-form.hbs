<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- CSS -->
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    <style>
        #jumb {background-color: white; margin-left: 3cm;}
        #añadir {margin-top: 0.625cm;}
        #conf {margin-bottom: 10cm; margin-left: 0.3cm; margin-top: -1.225cm;}
        #p {margin-left:7.2cm}
        #foto2 {width: 40px; height: 35px; margin-bottom: 0.2cm; background-color: white; color: white;}
        #hh {background-color: white; color: white; margin-left: 47.5cm; margin-top: 0.38cm;}
        .container {margin-top: -4.4cm}
        #pepe {margin-top: 5cm;}
    </style>
    <title>Nuevo pedido</title>
</head>
<body>
    <a  class="btn btn-sm" href="/logout"  id="hh" role="button"><img src="/images/logout.PNG" id="foto2"></a>
    <div class="container">
        <header class="jumbotron" id="jumb">
            <!-- Logo -->
           <img src="/images/logo.PNG" id="foto">
           
           <h1  id="p">Nuevo pedido</h1>
        </header>

        <section>
            <!--Fármacos selecionados-->
            <table class="table">
                <thead class="thead-dark">
                    <tr>
                        <td scope="col">Cantidad</td>
                        <td scope="col">Fármaco</td>
                    </tr>
                </thead>
                <tbody>
                    {{#each pedido.Asignacions}}
                        <tr scope="row">
                            <td>{{cantidad}}</td>
                            <td>{{Farmaco.nombre}}</td>
                        </tr>
                     {{/each}}
                </tbody>
            </table>
        </section>
    
        <!--Formulario para añadir fármacos al nuevo pedido-->
        <section class="container">
            <center>
                <form action="" method="post" class="form-inline" id="pepe">
                    <div class="form-row align-items-center">
                        <div class="col-auto">
                            <label for="cantidad" >Cantidad</label>
                            <input type="number" name="cantidad" id="cantidad" max="5" class="form-control mb-2">
                        </div>
                        <div class="col-auto">
                            <label for="farmaco">Fármaco</label>
                             <select name="farmaco" class="form-control mb-2">
                                {{#each farmaco}}
                                    <option value="{{id}}">{{nombre}}</option>
                                {{/each}}
                            </select>
                        </div>
                        <div class="col-auto" id="añadir">
                            <input type="submit" class="btn btn-primary mb-2" value="Añadir">
                        </div>
                    </div>
                </form>
        </center>

                <center >
                    <form action="/users/{{usuario.id}}/nuevo-pedido/{{pedido.id}}/checkout" method="POST" >
                        <input type="submit" class="btn btn-primary" value="Confirmar pedido" id="conf">
                    </form>
                </center>
            
        </section>
    </div>
    
</body>
</html>